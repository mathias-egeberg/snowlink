# SnowLink - Raspberry Pi Requirements
# Tested on Raspberry Pi OS Bullseye / Bookworm / Trixie
#
# The map renderer on Pi is pywebview (WebKitGTK 4.x). PyGObject and pycairo
# build from source against the GI / cairo dev packages installed by setup.sh.
# PyGObject is pinned <3.52 because >=3.52 requires libgirepository-2.0-dev
# which is not available on Bookworm.

kivy==2.3.0
Pillow>=10.0.0
httpx>=0.24.0
pywebview>=4.4
PyGObject>=3.42,<3.52
pycairo>=1.20
